language: cpp
compiler: clang
addons: 
  apt: 
    sources: 
      - sourceline: 'ppa:snaipewastaken/ppa'
    packages:
      - criterion-dev
      - clang
      - libboost-all-dev
    update: true
install:
  - make build
  - make
script:
  - ./build/test
