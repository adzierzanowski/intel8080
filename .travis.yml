os: osx
language: cpp
compiler: clang
addons: 
  homebrew: 
    packages:
      - cmake
      - snaipe/soft/criterion
      - boost
    update: true
install:
  - git submodule update --init
  - ls -l
  - make all
  - ls build

script:
  - ./build/test
